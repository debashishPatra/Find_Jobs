# For local development, leave this unset (defaults to http://localhost:8080/api)
# For production on Netlify, set this in Netlify's Site settings > Environment variables:
# REACT_APP_API_URL=https://your-backend-name.onrender.com/api
